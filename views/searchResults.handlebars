<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Clothes Search Results</title>
</head>
<body>
    <h1>Clothes Search Results</h1>

    {{#if searchResults}}
        <ul>
            {{#each searchResults}}
                <li>
                    <h2>{{title}}</h2>
                    <p><strong>Seller:</strong> {{seller}}</p>
                    <p><strong>Description:</strong> {{description}}</p>
                    <p><strong>Article:</strong> {{article}}</p>
                    <p><strong>Size:</strong> {{size}}</p>
                    <p><strong>Color:</strong> {{color}}</p>
                    <p><strong>Gender:</strong> {{gender}}</p>
                    <p><strong>Price:</strong> ${{price}}</p>
                    <p><strong>Condition:</strong> {{condition}}</p>
                    <p><strong>Tags:</strong> {{#each tags}}{{this}}, {{/each}}</p>
                    <p><strong>Photos:</strong> {{#each photos}}{{this}}, {{/each}}</p>
                    <p><strong>Comments:</strong> {{#if comments.length}}{{#each comments}}{{this}}, {{/each}}{{else}} No comments{{/if}}</p>
                    <p><strong>Rating:</strong> {{rating}}</p>
                </li>
            {{/each}}
        </ul>
    {{else}}
        <p>No results found.</p>
    {{/if}}
</body>
</html>
