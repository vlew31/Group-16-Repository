<div class="listing">
  <h4>{{ listing.title }}</h4>
    <div class="photos-container center-images">
      {{#each listing.photos}}
        <img class="small-photo" src="{{ this }}" alt="Listing Photo">
      {{/each}}
    </div>
  <p><span class="key">Seller:</span> <span class="value">{{ listing.seller }}</span></p>
  <p><span class="key">Description:</span> <span class="value">{{ listing.description }}</span></p>
  <p><span class="key">Article:</span> <span class="value">{{ listing.article }}</span></p>
  <p><span class="key">Size:</span> <span class="value">{{ listing.size }}</span></p>
  <p><span class="key">Color:</span> <span class="value">{{ listing.color }}</span></p>
  <p><span class="key">Gender:</span> <span class="value">{{ listing.gender }}</span></p>
  <p><span class="key">Price:</span> <span class="value">${{ listing.price }}</span></p>
  <p><span class="key">Condition:</span> <span class="value">{{ listing.condition }}</span></p>
  <p><span class="key">Comments:</span> <span class="value">{{ listing.comments }}</span></p>
  <p><span class="key">Tags:</span> 
    {{#each listing.tags}}
      <span class="tag">{{ this }}</span>
    {{/each}}
  </p>
  <a href="/cart" class="button">Add to Cart</a>
  <a href="/cart" class="button">Add to Wishlist</a>
  <a href="/update/{{ listing._id }}" class="button">Update Listing</a>
  <a href="/" class = "button">Delete Listing</a>

    {{!-- <form id="updateForm" action="/update/${listing._id}" method="POST">
    <!-- Add hidden input fields to hold listing details -->
    <input type="hidden" name="id" value="{{listing._id}}">
    <input type="hidden" name="seller" value="{{listing.seller}}">
    <input type="hidden" name="title" value="{{listing.title}}">
    <input type="hidden" name="description" value="{{listing.description}}">
    <input type="hidden" name="article" value="{{listing.article}}">
    <input type="hidden" name="size" value="{{listing.size}}">
    <input type="hidden" name="color" value="{{listing.color}}">
    <input type="hidden" name="gender" value="{{listing.gender}}">
    <input type="hidden" name="price" value="{{listing.price}}">
    <input type="hidden" name="condition" value="{{listing.condition}}">
    <input type="hidden" name="photos" value="{{listing.photos}}">
    <input type="hidden" name="tags" value="{{tags}}">
    <!-- Submit button -->
    <input type="submit" class = 'button 'value="update listing">
    </form> --}}
</div>