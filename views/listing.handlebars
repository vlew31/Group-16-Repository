<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
</head>
<body>
    <h2>{{ listing.title }}</h2>
    {{#each listing.photos}}
        <img src="{{this}}" alt="Photo" style="max-width: 300px; max-height: 300px;">
    {{/each}}
    <p><strong>id:</strong> {{ listing._id }}</p>
    <p><strong>Seller:</strong> {{ listing.seller }}</p>
    <p><strong>Description:</strong> {{ listing.description }}</p>
    <p><strong>Article:</strong> {{ listing.article }}</p>
    <p><strong>Size:</strong> {{ listing.size }}</p>
    <p><strong>Color:</strong> {{ listing.color }}</p>
    <p><strong>Gender:</strong> {{ listing.gender }}</p>
    <p><strong>Price:</strong> ${{ listing.price }}</p>
    <p><strong>Condition:</strong> {{ listing.condition }}</p>
    <p><strong>Tags:</strong> {{#each listing.tags}}{{ this }}, {{/each}}</p>
    <p><strong>Comments:</strong> {{#if listing.comments.length}}{{#each listing.comments}}{{ this }}, {{/each}}{{else}} No comments{{/if}}</p>
    <p><strong>Rating:</strong> {{ listing.rating }}</p>
    <a href="/cart" class="button">Add to Cart</a>
    {{!-- <a href="/update" class="button">update listing</a> --}}
    {{!-- <form id="updateForm" action="/update" method="POST">
      <!-- Add hidden input fields to hold listing details -->
      <input type="hidden" name="id" value="{{listing._id}}">
      <input type="hidden" name="title" value="{{listing.title}}">
      <input type="hidden" name="photos" value="{{listing.photos}}">
      <input type="hidden" name="description" value="{{listing.description}}">
      <input type="hidden" name="article" value="{{listing.article}}">
      <input type="hidden" name="size" value="{{listing.size}}">
      <input type="hidden" name="color" value="{{listing.color}}">
      <input type="hidden" name="gender" value="{{listing.gender}}">
      <input type="hidden" name="price" value="{{listing.price}}">
      <input type="hidden" name="condition" value="{{listing.condition}}">
      <input type="hidden" name="tags" value="{{listing.tags}}">
      <!-- Submit button --> --}}
      <form id="updateForm" action="/update" method="POST">
        <!-- Add hidden input fields to hold listing details -->
        <input type="hidden" name="id" value="{{listing._id}}">
        <input type="hidden" name="seller" value="{{listing.seller}}">
        <input type="hidden" name="title" value="{{listing.title}}">
        <input type="hidden" name="description" value="{{listing.description}}">
        <input type="hidden" name="article" value="{{listing.article}}">
        <input type="hidden" name="size" value="{{listing.size}}">
        <input type="hidden" name="color" value="{{listing.color}}">
        <input type="hidden" name="gender" value="{{listing.gender}}">
        <input type="hidden" name="price" value="{{listing.price}}">
        <input type="hidden" name="condition" value="{{listing.condition}}">
        <input type="hidden" name="photos" value="{{listing.photos}}">
        <input type="hidden" name="tags" value="{{tags}}">
        <!-- Submit button -->
        <input type="submit" class = 'button 'value="update listing">
        </form>
  </form>
</body>
</html>
